// Public type surface of @docsmith/designer. Importing the package for its side
// effect (registering <doc-designer>) is done via the built `doc-designer.js`
// bundle (see main.ts) — this module only re-exports types for consumers writing
// TypeScript against the SDK.
export type { DocDesignerConfig } from './types.js';
